extern crate rand;


fn for_string_rec_aux(word: String,
                      used: Vec<Boolean>,
                      mut perms: Vec<String>) -> Vec<String> {

    perms
}

pub fn for_string_rec(word: String) -> Vec<String> {

    let mut perms = Vec::new();
    for_string_rec_aux(word, perms)

}
